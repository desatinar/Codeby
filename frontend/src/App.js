import { Header } from "./components/Header";
import { Router } from "./router/Router";
import { AppContainer } from "./style"

export const App = () => {
  return (
    <div>
      <Router/>
    </div>
  );
}

export default App;
